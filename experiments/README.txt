The files in this directory are various test models for prediction, written in
Python and using Google's TensorFlow library.  None of them was able to achieve
comparable accuracy to the Naive Bayes classifier in the main directory. 

To run these tests, you need to move the files to the main directory, where
they will be able to find the input data correctly.
